# テストカバレッジ分析とテスト追加計画

## 現在のカバレッジ状況

### モジュール別カバレッジ
```
requests.py          █████████████████░░░  86%
utils.py             ███████████████████░  95%
hlsfile.py           ██████████████░░░░░░  72%
webfile.py           ████████████░░░░░░░░  64%
webpage.py           █████████░░░░░░░░░░░  46%
---
全体                  ████████████░░░░░░░░  60%
```

### テスト実行結果
- **成功**: 66テスト
- **失敗**: 50テスト
- **スキップ**: 77テスト（Selenium関連）

### 失敗の主な原因
1. **ネットワークエラー**: httpbin.orgへのアクセス失敗によるリトライエラー
2. **FFmpeg不在**: HlsFileのダウンロードテストが失敗
3. **外部リソース**: テストHTMLページの取得失敗

---

## リファクタリング対象のテストカバレッジ

### 優先度：高（すぐに対応すべき）

#### 1. 状態検証ガードの抽出 ✅ 部分的にカバー
**対象コード**:
- webfile.py: `if self.response is None` チェック
- hlsfile.py: 状態チェック
- webpage.py: `if self.driver is None` チェック

**既存テスト**:
- `test_webfile.py::test_seek_error` - closeされた状態でのseek呼び出し

**不足しているテスト**:
- [ ] WebFile: openせずにread()を呼ぶ
- [ ] HlsFile: openせずにread()を呼ぶ
- [ ] WebPage: openせずにget()を呼ぶ

---

#### 2. HlsFile.clear_cache() の修正 ❌ テストなし
**対象コード**: hlsfile.py:122-142

**既存テスト**: なし

**必要なテスト**:
- [ ] clear_cache()を呼んでキャッシュされたプロパティがクリアされることを確認
- [ ] URL変更時に自動でキャッシュがクリアされることを確認
- [ ] キャッシュクリア後に再度プロパティアクセスで再計算されることを確認

---

#### 3. get()メソッドの統合 ✅ カバー済み
**対象コード**:
- webpage.py:94 - WebPageParserMixin.get()
- webpage.py:340 - WebPageSelenium.get()
- webpage.py:446 - SeleniumWebPageElement.get()

**既存テスト**:
- `test_webpage.py::MixinTestWebPage::test_get_01` - 基本的なget()
- `test_webpage.py::MixinTestWebPage::test_get_02` - 要素が見つからない場合
- `test_webpage.py::MixinTestWebPageSelenium::test_get_timeout_*` - タイムアウト

**不足しているテスト**:
- [ ] WebPageRequests.get()のタイムアウトパラメータ（現在は無視される）
- [ ] 異なる実装間での一貫性テスト

---

### 優先度：中（次のフェーズで対応）

#### 4. Seleniumドライバークラスの統合 ⚠️ 環境依存
**対象コード**:
- webpage.py:574-684 - WebPageFirefox/WebPageChrome

**既存テスト**:
- Selenium関連テストは存在するが、環境依存（ブラウザドライバー必要）

**不足しているテスト**:
- [ ] プロキシ設定のユニットテスト（モック使用）
- [ ] ドライバーオプションのユニットテスト（モック使用）

---

#### 5. WebPageSeleniumの分解 ⚠️ 環境依存
**対象コード**: webpage.py:269-569 - WebPageSelenium（208行）

**既存テスト**:
- Selenium関連テストは環境依存

**不足しているテスト**:
- [ ] 各機能のユニットテスト（モック使用）

---

### 優先度：低（磨き上げ）

#### 6. LazyListクラス ❌ テストなし
**対象コード**: utils.py:109-146 - LazyList

**既存テスト**: なし

**必要なテスト**:
- [ ] 基本的なリスト操作（len, getitem, contains）
- [ ] 遅延評価の動作確認
- [ ] キャッシュの動作確認

---

## 追加すべきテストの優先順位

### Phase 1: クリティカルなテスト（リファクタリング前に必須）

1. **HlsFile.clear_cache()のテスト** - 2時間
   - clear_cache()の基本動作
   - URL変更時の自動クリア
   - 再計算の確認

2. **状態検証エラーのテスト** - 1時間
   - WebFile/HlsFileのopen前のメソッド呼び出し
   - 適切なエラーメッセージの確認

3. **LazyListのテスト** - 1時間
   - 基本的なリスト操作
   - 遅延評価とキャッシュ

### Phase 2: 環境非依存のユニットテスト（推奨）

4. **モックを使ったSeleniumテスト** - 3-4時間
   - プロキシ設定のロジック
   - ドライバーオプションの構築
   - エラーハンドリング

5. **WebFileのダウンロードロジック** - 2-3時間
   - モックを使った進捗コールバック
   - 一時ファイル処理
   - Range requestのロジック

### Phase 3: 統合テストの改善（長期）

6. **テストフィクスチャの改善** - 2-3時間
   - ローカルテストサーバーの構築
   - 外部依存の削減
   - テストの安定化

---

## 推奨アクション

### 即座に実施
1. ✅ HlsFile.clear_cache()のテストを追加
2. ✅ 状態検証エラーのテストを追加
3. ✅ LazyListのテストを追加

### 短期的（リファクタリング前）
4. モックを使ったユニットテストの拡充
5. テストの安定化（外部依存の削減）

### 中長期的
6. テストカバレッジを80%以上に向上
7. 統合テストの環境を整備
8. CI/CDでの自動テスト実行

---

## テスト追加の見積もり

| タスク | 優先度 | 工数 | カバレッジ向上 |
|--------|--------|------|----------------|
| clear_cache()テスト | 高 | 2h | +5% |
| 状態検証テスト | 高 | 1h | +3% |
| LazyListテスト | 中 | 1h | +4% |
| モックSeleniumテスト | 中 | 4h | +8% |
| ダウンロードテスト | 中 | 3h | +6% |
| **合計** | - | **11h** | **+26%** |

**期待結果**: カバレッジ 60% → 86%

---

## 次のステップ

1. Phase 1のテストを追加（4時間）
2. テストが全てパスすることを確認
3. リファクタリングを安全に実施
4. リファクタリング後にテストを再実行して回帰がないことを確認
