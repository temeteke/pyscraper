[metadata]
name = pyscraper
author = temeteke
author_email = temeteke@gmail.com

[options]
packages=find:
use_scm_version = True
install_requires =
    requests
    selenium
    lxml
    retry
    tqdm
    ffmpy
    m3u8
    fake-useragent
    # Pinned: must stay in sync with Dockerfile.playwright-node and
    # Dockerfile.playwright-session-manager ARG PLAYWRIGHT_VERSION
    # (client and node must speak the same Playwright protocol version).
    playwright==1.62.0
